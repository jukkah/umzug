sudo: required
dist: trusty

language: node_js

node_js:
  - "0.12"
  - "4"
  - "6"
  - "7"

env:
  - SEQUELIZE=^1.0.0
  - SEQUELIZE=^2.0.0
  - SEQUELIZE=^3.0.0

after_success: "npm run coveralls"

before_script:
  - npm install sequelize@$(echo $SEQUELIZE)

script:
#  - "npm run lint"
  - "npm run coverage"

notifications:
  irc:
    - "chat.freenode.net#sequelizejs"
